apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: iotstack
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/iotstack: "true"
  podSelector:
    matchLabels:
      io.kompose.network/iotstack: "true"
